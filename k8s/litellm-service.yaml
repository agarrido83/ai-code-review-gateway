apiVersion: v1
kind: Service
metadata:
  name: litellm-service
  namespace: ai-gateway
  labels:
    app: litellm
    component: gateway
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
